# Frontend configuration. Copy to .env and fill in.

# Base URL of the retriever API. Must be reachable from the browser, and must
# appear in the API's ALLOWED_ORIGINS list the other way round.
NEXT_PUBLIC_API_URL=http://localhost:8000
